module OS.Linux.Debian where

import           Prelude                        ( IO )
import           Data.Text.IO                   ( putStrLn )


debianAtom :: IO ()
debianAtom = putStrLn "not yet!"
